---
phase: 03-simulation-capabilities
plan: 06
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified: []
autonomous: false
requirements:
  - SIM-01
  - SIM-02
  - SIM-03
  - SIM-04
  - MAT-01
  - MAT-02
  - MAT-03
must_haves:
  truths:
    - "All seven Phase 3 requirements verified by human"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all Phase 3 Simulation Capabilities requirements.

Purpose: Visual and functional verification that all seven requirements work correctly in both CLI and GUI before marking Phase 3 complete.

Output: Human confirmation that all success criteria pass.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-simulation-capabilities/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all Phase 3 Simulation Capabilities requirements</name>
  <files></files>
  <action>
Launch the GUI and CLI to verify all 7 requirements interactively. Complete Phase 3 Simulation Capabilities: parametric sweep engine with GUI dialog and results display, time-varying power profiles with breakpoint editor, material library import/export with built-in vs user distinction, and expanded analytical validation benchmarks.
  </action>
  <verify>
    <automated>cd G:/blu-thermal-simulation && py -m pytest -q tests/ -x</automated>
  </verify>
  <done>All 7 Phase 3 requirements confirmed working by human tester</done>
  <how-to-verify>
Launch the GUI: `python -m thermal_sim.app.gui`

**SIM-01 -- Parametric Sweep Definition and Execution:**
1. Load an example project (e.g., `examples/localized_hotspots_stack.json`)
2. Open the Parametric Sweep dialog (Simulation menu or toolbar)
3. Select "Layer thickness" as the parameter category
4. Select a specific layer from the target dropdown
5. Enter values: "0.001, 0.002, 0.003, 0.005" in the values field
6. Select "steady" mode
7. Click OK and verify the sweep starts running
8. Verify progress shows "Run 1 of 4", "Run 2 of 4", etc. in the status bar
9. Verify the GUI does NOT freeze during sweep execution
10. Also test CLI sweep: `python -m thermal_sim.app.cli --project examples/localized_hotspots_stack.json --mode steady --sweep test_sweep.json` (create a test_sweep.json first)

**SIM-02 -- Sweep Results Display:**
11. After sweep completes, switch to the Sweep Results tab
12. Verify comparison table shows 4 rows with Parameter Value, T_max, and T_avg columns per layer
13. Verify the parameter-vs-metric plot shows a line/scatter plot
14. Change the layer dropdown in sweep results -- verify the plot updates
15. Change the metric dropdown (T_max / T_avg) -- verify the plot updates
16. Click Export CSV -- verify a CSV file is saved with the table data
17. Click Export PNG -- verify a PNG image is saved with the current plot

**SIM-03 -- Time-Varying Power Profiles:**
18. Go to the Heat Sources tab
19. Select a heat source row
20. Check the "[x] Time-varying" checkbox
21. Verify a breakpoint table appears below with Time [s] and Power [W] columns
22. Add breakpoints: (0, 2.0), (0.5, 0.0), (1.0, 2.0) for a square-wave duty cycle
23. Verify the preview plot below the table updates to show a piecewise-linear profile
24. Edit a breakpoint value -- verify the preview plot updates live

**SIM-04 -- Transient Solver Power Profile Interpolation:**
25. With the power profile defined in step 22, switch mode to "transient"
26. Set transient parameters: dt=0.05s, total_time=3.0s
27. Run the simulation
28. Verify the probe temperature history (if probes defined) shows oscillating temperature matching the duty cycle pattern
29. Verify the final temperature is NOT the same as a constant-power simulation (confirms profile is being used)

**MAT-01 -- Material Library Import/Export:**
30. Go to the Materials tab
31. Click "Load Presets" -- verify built-in materials appear (Copper, Aluminum, Glass, FR4, etc.)
32. Add a custom material row (e.g., "MyAlloy" with custom properties)
33. Select the custom material row and click "Export" -- verify a JSON file is saved
34. Remove the custom material row
35. Click "Import" and select the exported JSON file -- verify "MyAlloy" reappears in the table
36. Click "Import" again with the same file -- verify the name is auto-renamed to "MyAlloy_imported" (conflict resolution) and a message appears

**MAT-02 -- Built-in vs User Material Distinction:**
37. Verify the Materials table has a "Type" column
38. Verify built-in materials (from Load Presets) show "Built-in" in the Type column
39. Verify custom/imported materials show "User" in the Type column
40. Try to edit a built-in material's property cell -- verify it is protected (read-only or creates a clone)
41. Verify user materials are fully editable

**MAT-03 -- Analytical Validation Test Cases:**
42. Run the validation test suite: `py -m pytest tests/test_validation_cases.py -v`
43. Verify at least 4 new test functions pass (in addition to the existing 1D two-layer test):
    - test_1d_three_layer_resistance_chain_matches_hand_calculation
    - test_single_node_rc_with_square_wave_power_matches_analytical
    - test_two_node_lateral_spreading_matches_hand_calculation
    - test_constant_power_profile_matches_no_profile
44. Generate validation comparison plots and verify at least 3 PNG images are created showing analytical vs numerical results

**Also verify:**
45. Run a transient sweep (change mode to "transient" in sweep dialog) -- verify it works with progress
46. Run the full test suite: `py -m pytest -q tests/` -- verify all tests pass
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all seven Phase 3 requirements work correctly in both CLI and GUI.
</verification>

<success_criteria>
- SIM-01: User can define and execute a parametric sweep with "Run N of M" progress; works from both CLI and GUI
- SIM-02: Sweep results display as comparison table + parameter-vs-metric plot with export buttons
- SIM-03: Power profile breakpoint editor with live preview in Heat Sources tab
- SIM-04: Transient solver correctly uses time-varying power profiles at each timestep
- MAT-01: Material JSON import/export works with name conflict resolution
- MAT-02: Materials tab distinguishes Built-in vs User with appropriate edit protection
- MAT-03: At least 3 new analytical validation tests pass with comparison plots
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-capabilities/03-06-SUMMARY.md`
</output>
