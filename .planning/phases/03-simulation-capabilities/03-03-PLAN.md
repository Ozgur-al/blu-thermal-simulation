---
phase: 03-simulation-capabilities
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - thermal_sim/resources/materials_builtin.json
  - thermal_sim/resources/__init__.py
  - thermal_sim/core/material_library.py
  - thermal_sim/ui/main_window.py
  - tests/test_material_library.py
autonomous: true
requirements: [MAT-01, MAT-02]

must_haves:
  truths:
    - "Built-in materials are loaded from materials_builtin.json via importlib.resources"
    - "default_materials() still works as before (backward compatible)"
    - "User can import a JSON file of materials into the project"
    - "User can export selected materials to a JSON file"
    - "On import name conflict, incoming material is auto-renamed with _imported suffix and user is notified"
    - "Materials tab shows a Type column distinguishing Built-in vs User materials"
    - "Built-in material rows are read-only in the table"
    - "Editing a built-in material creates a user clone named 'Name (custom)'"
  artifacts:
    - path: "thermal_sim/resources/materials_builtin.json"
      provides: "JSON file with common display/automotive material presets"
      contains: "Copper"
    - path: "thermal_sim/core/material_library.py"
      provides: "load_builtin_library(), import_materials(), export_materials() functions"
      contains: "load_builtin_library"
    - path: "thermal_sim/ui/main_window.py"
      provides: "Materials tab with Type column, Import/Export buttons, built-in read-only rows, edit-to-clone behavior"
      contains: "Import"
    - path: "tests/test_material_library.py"
      provides: "Unit tests for library loading, import conflict resolution, export round-trip"
      contains: "test_import"
  key_links:
    - from: "thermal_sim/core/material_library.py"
      to: "thermal_sim/resources/materials_builtin.json"
      via: "load_builtin_library() reads the bundled JSON file"
      pattern: "materials_builtin"
    - from: "thermal_sim/ui/main_window.py"
      to: "thermal_sim/core/material_library.py"
      via: "Import/Export buttons call import_materials/export_materials"
      pattern: "import_materials"
---

<objective>
Build the material library system: a bundled JSON file of built-in presets, import/export functions, and GUI integration with a Type column distinguishing built-in vs user materials. Built-in rows are read-only; editing one creates a user clone.

Purpose: MAT-01 (import/export material JSON) and MAT-02 (material picker distinguishes built-in from user). These are independent of the sweep engine and power profiles, enabling parallel development.
Output: materials_builtin.json resource file, extended material_library.py, Materials tab with Type column and Import/Export buttons, unit tests.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simulation-capabilities/03-RESEARCH.md

@thermal_sim/core/material_library.py
@thermal_sim/models/material.py
@thermal_sim/ui/main_window.py
@thermal_sim/ui/table_data_parser.py

<interfaces>
<!-- Existing interfaces the executor needs -->

From thermal_sim/models/material.py:
```python
@dataclass(frozen=True)
class Material:
    name: str
    k_in_plane: float
    k_through: float
    density: float
    specific_heat: float
    emissivity: float = 0.9
    def to_dict(self) -> dict: ...
    @classmethod
    def from_dict(cls, data: dict) -> "Material": ...
```

From thermal_sim/core/material_library.py (current):
```python
def default_materials() -> dict[str, Material]:
    """Return a practical starter material library."""
    materials = [
        Material("Glass", ...),
        Material("OCA", ...),
        # ... 10 materials total
    ]
    return {item.name: item for item in materials}
```

From thermal_sim/ui/main_window.py — current Materials tab builder:
```python
def _build_materials_tab(self) -> QWidget:
    presets_btn = QPushButton("Load Presets")
    presets_btn.clicked.connect(self._load_default_materials)
    tab, self.materials_table = self._build_table_tab(
        ["Name", "k in-plane [W/mK]", "k through [W/mK]",
         "Density [kg/m³]", "Specific heat [J/kgK]", "Emissivity"],
        extra_buttons=[presets_btn],
    )
    self._wire_table_undo(self.materials_table)
    return tab
```

Materials tab headers will gain a new "Type" column (first or last position).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Built-in material JSON resource and library functions</name>
  <files>thermal_sim/resources/__init__.py, thermal_sim/resources/materials_builtin.json, thermal_sim/core/material_library.py, tests/test_material_library.py</files>
  <behavior>
    - Test: load_builtin_library() returns a dict[str, Material] with at least 10 materials including Copper, Aluminum, Glass, FR4
    - Test: load_builtin_library() materials round-trip through Material.to_dict()/from_dict()
    - Test: default_materials() still returns the same materials as before (backward compat)
    - Test: import_materials(existing_dict, incoming_dict, builtin_names) merges correctly: new names added directly
    - Test: import_materials with name conflict auto-renames incoming to "Name_imported" and returns list of renamed materials
    - Test: import_materials with conflict against a built-in name also renames
    - Test: export_materials(materials_dict, path) writes valid JSON that can be loaded back
    - Test: load_materials_json(path) loads a JSON file into dict[str, Material]
  </behavior>
  <action>
    1. Create thermal_sim/resources/__init__.py (empty file for package).
    2. Create thermal_sim/resources/materials_builtin.json with the same 10 materials from default_materials() plus additional common materials:
       - Keep: Glass, OCA, PMMA, PC, Aluminum, Steel, FR4, Copper, Thermal Pad, Graphite Sheet
       - Add: Solder (SnAgCu), BT Substrate, Polyimide, Silicone Rubber, Air Gap
       - Use engineering reference values (SI units). Each entry: {"name": ..., "k_in_plane": ..., "k_through": ..., "density": ..., "specific_heat": ..., "emissivity": ...}
       - Format: dict of name -> material properties dict
    3. In thermal_sim/core/material_library.py, add:
       - load_builtin_library() -> dict[str, Material]: loads materials_builtin.json via importlib.resources (from importlib.resources import files; path = files("thermal_sim.resources").joinpath("materials_builtin.json"))
       - load_materials_json(path: Path) -> dict[str, Material]: loads arbitrary material JSON file
       - export_materials(materials: dict[str, Material], path: Path) -> None: writes JSON
       - import_materials(existing: dict[str, Material], incoming: dict[str, Material], builtin_names: set[str]) -> tuple[dict[str, Material], list[str]]: merges incoming into existing; returns (merged dict, list of rename messages). For each incoming name that conflicts with existing or builtin, rename to "Name_imported". If "Name_imported" also exists, try "Name_imported_2", etc.
       - Keep default_materials() unchanged for backward compatibility
    4. Create tests/test_material_library.py with all test cases listed above
  </action>
  <verify>
    <automated>py -m pytest tests/test_material_library.py -x -q</automated>
  </verify>
  <done>Built-in materials JSON resource file created. Library functions load, import (with conflict rename), and export materials. Backward-compatible default_materials() preserved. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Materials tab GUI — Type column, Import/Export buttons, built-in protection</name>
  <files>thermal_sim/ui/main_window.py, thermal_sim/ui/table_data_parser.py</files>
  <behavior>
    After this task:
    - Materials table has a "Type" column showing "Built-in" or "User" for each row
    - Built-in rows are not directly editable (flags exclude Qt.ItemIsEditable on non-name columns)
    - Editing a built-in material's field triggers clone creation: new "Name (custom)" row appears as User type with the edited value; the built-in row is restored
    - Import button opens a file dialog, calls import_materials, adds rows, and shows a message if any were renamed
    - Export button opens a save dialog, exports selected materials (or all user materials if none selected)
    - Load Presets button still works, loading all built-in materials
    - The _material_source dict tracks which materials are "Built-in" vs "User"
  </behavior>
  <action>
    1. Add self._material_source: dict[str, str] = {} to MainWindow.__init__ — maps material name to "Built-in" or "User"
    2. Modify _build_materials_tab():
       - Add "Type" as the last column header
       - Add Import and Export QPushButtons alongside Load Presets
       - Wire Import to _import_materials_dialog()
       - Wire Export to _export_materials_dialog()
    3. Add _import_materials_dialog():
       - QFileDialog.getOpenFileName(filter="JSON (*.json)")
       - Call load_materials_json(path)
       - Call import_materials(existing, incoming, builtin_names)
       - Add new materials to the table with Type="User"
       - Show QMessageBox.information if any were renamed
       - Mark project dirty
    4. Add _export_materials_dialog():
       - Get selected rows; if none, export all "User" materials
       - QFileDialog.getSaveFileName(filter="JSON (*.json)")
       - Call export_materials(selected_dict, path)
    5. Modify _load_default_materials() (existing) to:
       - Use load_builtin_library() instead of default_materials()
       - Set Type column to "Built-in" for each loaded material
       - Store in self._material_source
       - Set flags on built-in rows to exclude Qt.ItemIsEditable for property columns
    6. Add clone-on-edit protection for built-in materials:
       - In the cellChanged handler (or via the undo system), when a built-in material cell is edited:
         a. Restore the original value in the built-in row
         b. Create a new row with name "OriginalName (custom)", Type="User", and the edited value
         c. Show a brief status bar message or tooltip indicating the clone
       - ALTERNATIVELY: simpler approach — set item flags to non-editable on built-in rows. User must use "Add" to create a new material if they want custom values. This is simpler and avoids complex clone logic. Choose this approach if the clone-on-edit is too entangled with the undo system.
    7. Update TableDataParser.populate_tables_from_project() to populate the Type column based on self._material_source (if it exists on the main_window reference)
  </action>
  <verify>
    <automated>py -m pytest -q tests</automated>
    <manual>Launch GUI, click Load Presets — verify Built-in type shown. Try editing a built-in row — verify it is protected. Add a custom material — verify User type. Import a JSON file — verify materials appear with correct types.</manual>
  </verify>
  <done>Materials tab shows Type column, Import/Export buttons work, built-in materials are protected from editing, user materials are editable. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. All existing tests pass: `py -m pytest -q tests`
2. Material library tests pass: `py -m pytest tests/test_material_library.py -x -q`
3. GUI launches without error: `python -m thermal_sim.app.gui` (manual check)
4. Materials JSON round-trip: load_builtin_library() produces same materials as default_materials()
</verification>

<success_criteria>
- materials_builtin.json contains 10+ common engineering materials with correct SI property values
- load_builtin_library() returns all built-in materials
- Import resolves name conflicts with _imported suffix and notifies user
- Export writes valid JSON that can be re-imported
- Materials tab Type column correctly shows Built-in vs User
- Built-in rows are protected from direct editing
- All pre-existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-capabilities/03-03-SUMMARY.md`
</output>
