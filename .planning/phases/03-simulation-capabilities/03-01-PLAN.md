---
phase: 03-simulation-capabilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thermal_sim/models/heat_source.py
  - thermal_sim/solvers/network_builder.py
  - thermal_sim/solvers/transient.py
autonomous: true
requirements: [SIM-03, SIM-04]

must_haves:
  truths:
    - "HeatSource with power_profile produces time-varying power via power_at_time(t)"
    - "HeatSource without power_profile behaves identically to before (backward compatible)"
    - "Power profile shorter than simulation time loops from the beginning"
    - "Transient solver applies per-timestep power from profiles"
    - "Steady-state solver is unaffected by the b_vector split"
  artifacts:
    - path: "thermal_sim/models/heat_source.py"
      provides: "PowerBreakpoint dataclass, power_profile field on HeatSource, power_at_time method"
      contains: "class PowerBreakpoint"
    - path: "thermal_sim/solvers/network_builder.py"
      provides: "Split b_vector into b_boundary + b_sources with backward-compatible b_vector property"
      contains: "b_boundary"
    - path: "thermal_sim/solvers/transient.py"
      provides: "Per-timestep power scaling in time-step loop"
      contains: "power_at_time"
  key_links:
    - from: "thermal_sim/solvers/transient.py"
      to: "thermal_sim/models/heat_source.py"
      via: "power_at_time(t) called per timestep"
      pattern: "power_at_time"
    - from: "thermal_sim/solvers/network_builder.py"
      to: "thermal_sim/solvers/steady_state.py"
      via: "b_vector property preserves backward compatibility"
      pattern: "b_vector"
---

<objective>
Add time-varying power profile support to heat sources and refactor the thermal network builder to separate heat-source forcing from boundary forcing, enabling per-timestep power scaling in the transient solver.

Purpose: SIM-03 (duty-cycle power profiles) and SIM-04 (transient solver interpolation) are the physics foundation that all other Phase 3 features build on. The b_vector split is also needed for correct sweep behavior with transient + power profiles.
Output: PowerBreakpoint dataclass, HeatSource.power_at_time() method, ThermalNetwork with split b_boundary/b_sources, transient solver per-step power scaling.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-simulation-capabilities/03-RESEARCH.md

@thermal_sim/models/heat_source.py
@thermal_sim/solvers/network_builder.py
@thermal_sim/solvers/transient.py
@thermal_sim/solvers/steady_state.py
@tests/test_validation_cases.py

<interfaces>
<!-- Existing interfaces the executor needs -->

From thermal_sim/models/heat_source.py:
```python
@dataclass
class HeatSource:
    name: str
    layer: str
    power_w: float
    shape: ShapeType = "rectangle"
    x: float = 0.0
    y: float = 0.0
    width: float | None = None
    height: float | None = None
    radius: float | None = None

    def to_dict(self) -> dict: ...
    @classmethod
    def from_dict(cls, data: dict) -> "HeatSource": ...
```

From thermal_sim/solvers/network_builder.py:
```python
@dataclass(frozen=True)
class ThermalNetwork:
    a_matrix: csr_matrix
    b_vector: np.ndarray    # Currently single combined vector
    c_vector: np.ndarray
    grid: Grid2D
    n_layers: int
    layer_names: list[str]

def build_thermal_network(project: DisplayProject) -> ThermalNetwork: ...
def _apply_heat_sources(project, grid, b_vec, node_index_fn) -> None: ...
```

From thermal_sim/solvers/transient.py (time-step loop, lines 87-90):
```python
for step in range(1, n_steps + 1):
    np.multiply(c_over_dt, t_vec, out=rhs)
    rhs += network.b_vector    # <-- must change to use split vectors + power scaling
    t_vec = lu.solve(rhs)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PowerBreakpoint dataclass and power_at_time to HeatSource</name>
  <files>thermal_sim/models/heat_source.py, tests/test_power_profile.py</files>
  <behavior>
    - Test: HeatSource without power_profile returns constant power_w from power_at_time(t) for any t
    - Test: HeatSource with power_profile [(0, 10), (1, 0)] returns 10.0 at t=0, 5.0 at t=0.5, 0.0 at t=1.0
    - Test: Power profile loops: profile [(0, 10), (1, 0)] at t=1.5 returns 5.0 (same as t=0.5)
    - Test: Profile with single breakpoint [(0, 5)] returns 5.0 for any t (period=0 edge case, use power_w fallback)
    - Test: to_dict includes power_profile when present, omits or null when absent
    - Test: from_dict with power_profile key round-trips correctly
    - Test: from_dict without power_profile key produces power_profile=None (backward compatible)
  </behavior>
  <action>
    1. Add PowerBreakpoint dataclass with time_s: float and power_w: float fields, plus to_dict/from_dict.
    2. Add optional field to HeatSource: power_profile: list[PowerBreakpoint] | None = None
    3. Add method power_at_time(self, t: float) -> float on HeatSource:
       - If power_profile is None or empty, return self.power_w
       - If profile has only one point or last time_s <= 0, return self.power_w
       - Compute profile_end = self.power_profile[-1].time_s
       - t_wrapped = t % profile_end
       - Use np.interp(t_wrapped, times, powers) to interpolate
       - Import numpy at module level (add to imports)
    4. Update HeatSource.to_dict() to include power_profile key (list of breakpoint dicts, or None).
    5. Update HeatSource.from_dict() to parse power_profile if present, default None.
    6. Enforce first breakpoint time_s=0 in __post_init__ if power_profile is not None and not empty.
  </action>
  <verify>
    <automated>py -m pytest tests/test_power_profile.py -x -q</automated>
  </verify>
  <done>HeatSource supports optional power_profile with piecewise-linear interpolation and looping; to_dict/from_dict round-trip; existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split b_vector in ThermalNetwork and add per-timestep power to transient solver</name>
  <files>thermal_sim/solvers/network_builder.py, thermal_sim/solvers/transient.py, tests/test_power_profile.py</files>
  <behavior>
    - Test: Existing 1D two-layer steady-state validation test still passes (backward compat of b_vector property)
    - Test: Single-node transient with constant power_profile produces same result as without profile (Benchmark D from RESEARCH.md)
    - Test: Single-node RC transient with square-wave profile [(0, Q), (T_on, 0), (T_period, Q)] matches analytical piecewise-exponential solution within 1% for 1-second timestep over 10 seconds (Benchmark B from RESEARCH.md)
  </behavior>
  <action>
    1. In network_builder.py, change ThermalNetwork to have b_boundary and b_sources instead of b_vector:
       - b_boundary: np.ndarray (boundary contributions: g * T_amb for top/bottom/side)
       - b_sources: np.ndarray (heat source contributions at nominal power_w)
       - Add @property b_vector that returns b_boundary + b_sources for backward compatibility
    2. In build_thermal_network(), track b_boundary and b_sources as separate arrays:
       - Initialize both as zeros
       - All boundary contributions (g_top * ambient, g_bot * ambient, side * ambient) go into b_boundary
       - _apply_heat_sources writes into b_sources (pass b_sources instead of b_vec)
       - Return ThermalNetwork with b_boundary=b_boundary, b_sources=b_sources
    3. In transient.py, modify the time-step loop:
       - Before the loop, build a function or inline logic to compute scaled b_power at time t:
         - For each heat source in project.expanded_heat_sources(), if it has a power_profile, compute scale = source.power_at_time(t) / source.power_w (handle power_w=0 edge case)
         - Build a per-source contribution array by re-calling _apply_heat_sources-like logic, or more efficiently: store per-source node masks and nominal contributions from network_builder and scale them
       - SIMPLER APPROACH (recommended): Add a helper function _build_power_vector(project, network, t) that rebuilds the heat source b_vector from scratch at time t, using power_at_time(t) for each source. This is clean and correct. Performance is fine because _apply_heat_sources is O(n_sources * n_cells) which is trivial vs the splu solve.
       - In the loop: replace `rhs += network.b_vector` with `rhs += network.b_boundary + _build_power_vector(project, network, t_current)` where t_current = step * dt
       - For sources WITHOUT power_profile, power_at_time(t) returns constant power_w, so the rebuild produces the same vector as b_sources -- correctness guaranteed.
    4. Expose _apply_heat_sources (or a variant) so transient.py can call it. Options:
       - Make _apply_heat_sources importable (remove leading underscore or keep private and import anyway)
       - Or add a public build_heat_source_vector(project, grid, node_index_fn, time_s=None) function in network_builder that creates a fresh b_sources at a given time
    5. Steady-state solver uses network.b_vector (property) -- NO changes needed there.
  </action>
  <verify>
    <automated>py -m pytest tests/test_validation_cases.py tests/test_power_profile.py -x -q</automated>
  </verify>
  <done>ThermalNetwork has split b_boundary/b_sources with backward-compatible b_vector property. Transient solver applies per-timestep power from profiles. Existing validation test passes. New power profile tests pass.</done>
</task>

</tasks>

<verification>
1. All existing tests pass: `py -m pytest -q tests`
2. Power profile tests pass: `py -m pytest tests/test_power_profile.py -x -q`
3. Steady-state validation unchanged: `py -m pytest tests/test_validation_cases.py -x -q`
</verification>

<success_criteria>
- HeatSource.power_at_time(t) works with and without power_profile
- Power profile loops correctly for t > profile duration
- ThermalNetwork.b_vector property returns same result as before split
- Transient solver produces correct time-varying temperatures with power profiles
- All pre-existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-simulation-capabilities/03-01-SUMMARY.md`
</output>
