---
phase: 05-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - thermal_sim/core/paths.py
  - thermal_sim/core/material_library.py
  - thermal_sim/ui/main_window.py
  - thermal_sim/app/gui.py
  - tests/test_paths.py
autonomous: true
requirements: [DIST-03]

must_haves:
  truths:
    - "Resource paths resolve correctly in dev mode (running from source)"
    - "material_library.py loads builtin JSON via paths.py instead of importlib.resources"
    - "GUI startup project loads from paths.get_examples_dir(), not bare relative Path"
    - "Output directory defaults to Documents/ThermalSim/outputs/ instead of cwd/outputs"
    - "File Open dialog defaults to examples/ dir on first use"
    - "Window title shows version string (Thermal Simulator v1.0)"
    - "Splash screen appears during GUI startup and dismisses when main window is ready"
    - "Crash handler catches unhandled exceptions and writes crash.log"
  artifacts:
    - path: "thermal_sim/core/paths.py"
      provides: "Centralized frozen vs dev path resolution"
      exports: ["get_resources_dir", "get_examples_dir", "get_output_dir", "get_crash_log_path"]
    - path: "tests/test_paths.py"
      provides: "Unit tests for path resolution in dev mode"
      min_lines: 20
    - path: "thermal_sim/app/gui.py"
      provides: "Splash screen, crash handler, deferred heavy imports"
      min_lines: 60
  key_links:
    - from: "thermal_sim/core/material_library.py"
      to: "thermal_sim/core/paths.py"
      via: "get_resources_dir()"
      pattern: "from thermal_sim\\.core\\.paths import get_resources_dir"
    - from: "thermal_sim/ui/main_window.py"
      to: "thermal_sim/core/paths.py"
      via: "get_examples_dir(), get_output_dir()"
      pattern: "from thermal_sim\\.core\\.paths import"
    - from: "thermal_sim/app/gui.py"
      to: "thermal_sim/core/paths.py"
      via: "get_crash_log_path()"
      pattern: "from thermal_sim\\.core\\.paths import"
---

<objective>
Create the centralized resource path module (`paths.py`), migrate all callers to use it, and rewrite `gui.py` with splash screen, crash handler, and version display.

Purpose: DIST-03 requires a single path resolution module that works in both frozen (PyInstaller) and dev modes. All existing code that uses bare relative paths or `importlib.resources` must be migrated. The gui.py rewrite adds the user-facing launch experience (splash, crash safety, version in title).

Output: `thermal_sim/core/paths.py` with tests, migrated `material_library.py` and `main_window.py`, rewritten `gui.py`.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-RESEARCH.md

@thermal_sim/core/material_library.py
@thermal_sim/app/gui.py
@thermal_sim/ui/main_window.py (lines 128-182, 885-920, 1143-1200)

<interfaces>
<!-- Key integration points the executor needs -->

From thermal_sim/core/material_library.py (line 18 — the importlib.resources call to migrate):
```python
resource = files("thermal_sim.resources").joinpath("materials_builtin.json")
text = resource.read_text(encoding="utf-8")
```

From thermal_sim/ui/main_window.py (line 173 — output dir default):
```python
self._output_dir = Path(settings.value("output_dir", str(Path.cwd() / "outputs")))
```

From thermal_sim/ui/main_window.py (lines 908-919 — startup project):
```python
def _load_startup_project(self) -> None:
    default_path = Path("examples/steady_uniform_stack.json")
    if default_path.exists():
        ...
```

From thermal_sim/ui/main_window.py (line 1146 — file open dialog):
```python
path_str, _ = QFileDialog.getOpenFileName(self, "Open Project", str(Path.cwd()), "JSON (*.json)")
```

From thermal_sim/ui/main_window.py (lines 887-892 — window title):
```python
base = "Thermal Simulator"
if self.current_project_path:
    base = f"{self.current_project_path.name} - Thermal Simulator"
```

From thermal_sim/app/gui.py (lines 27-53 — current main function):
```python
def main() -> None:
    try:
        from PySide6.QtWidgets import QApplication
        from qt_material import apply_stylesheet
    except ModuleNotFoundError as exc:
        print("PySide6 is required ...")
        raise SystemExit(2) from exc
    ...
    app = QApplication(sys.argv)
    apply_stylesheet(app, theme="dark_amber.xml")
    ...
    window = MainWindow()
    window.show()
    raise SystemExit(app.exec())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create paths.py and migrate all callers</name>
  <files>thermal_sim/core/paths.py, thermal_sim/core/material_library.py, thermal_sim/ui/main_window.py, tests/test_paths.py</files>
  <behavior>
    - _bundle_root() returns project root in dev mode (3 levels up from paths.py)
    - _exe_dir() returns project root in dev mode (same as _bundle_root for non-frozen)
    - get_resources_dir() returns thermal_sim/resources/ path that exists in dev mode
    - get_examples_dir() returns examples/ path that exists in dev mode
    - get_output_dir() returns Path.home()/Documents/ThermalSim/outputs/
    - get_crash_log_path() returns _exe_dir()/crash.log
    - All functions return Path objects
  </behavior>
  <action>
    1. Create `thermal_sim/core/paths.py` exactly as specified in RESEARCH.md Pattern 1:
       - `_bundle_root()`: `sys._MEIPASS` when frozen, else `Path(__file__).resolve().parent.parent.parent`
       - `_exe_dir()`: `Path(sys.executable).parent` when frozen, else same as `_bundle_root()`
       - `get_resources_dir()`: `_bundle_root() / "thermal_sim" / "resources"`
       - `get_examples_dir()`: `_exe_dir() / "examples"`
       - `get_output_dir()`: `Path.home() / "Documents" / "ThermalSim" / "outputs"`
       - `get_crash_log_path()`: `_exe_dir() / "crash.log"`
       - Add `APP_VERSION = "1.0"` constant at module level for version string injection.

    2. Create `tests/test_paths.py`:
       - Test each public function returns a Path
       - Test `get_resources_dir()` points to existing directory in dev mode
       - Test `get_examples_dir()` points to existing directory in dev mode
       - Test `get_output_dir()` contains "Documents/ThermalSim/outputs"
       - Test frozen mode with `monkeypatch` setting `sys.frozen=True` and `sys._MEIPASS`

    3. Migrate `thermal_sim/core/material_library.py`:
       - Remove `from importlib.resources import files` import
       - In `load_builtin_library()`, replace `files("thermal_sim.resources").joinpath(...)` with `from thermal_sim.core.paths import get_resources_dir; resource = get_resources_dir() / "materials_builtin.json"`
       - Keep the import at module top level: `from thermal_sim.core.paths import get_resources_dir`
       - Update the docstring to mention paths.py instead of importlib.resources

    4. Migrate `thermal_sim/ui/main_window.py` — add import at top: `from thermal_sim.core.paths import get_examples_dir, get_output_dir, APP_VERSION`
       - Line 173: Change `Path(settings.value("output_dir", str(Path.cwd() / "outputs")))` to `Path(settings.value("output_dir", str(get_output_dir())))`
       - Lines 887-889: Change `"Thermal Simulator"` to `f"Thermal Simulator v{APP_VERSION}"` in both places in `_update_title()`
       - Line 909: Change `Path("examples/steady_uniform_stack.json")` to `get_examples_dir() / "steady_uniform_stack.json"`
       - Line 1146: Change `str(Path.cwd())` in `_load_project_dialog` to `str(settings.value("last_open_dir", str(get_examples_dir())))` — and after a successful load, save `settings.setValue("last_open_dir", str(path.parent))` (per locked decision: defaults to examples/ on first use, remembers last-used)
       - Line 1184: Change `Path.cwd() / "project.json"` in `_save_project_as_dialog` to `get_examples_dir() / "project.json"` as fallback

    5. Verify existing tests still pass — the material library and solver tests should be unaffected since paths.py resolves identically in dev mode.
  </action>
  <verify>
    <automated>cd G:/blu-thermal-simulation && py -m pytest -q tests/test_paths.py tests/test_validation_cases.py tests/test_models.py -x</automated>
  </verify>
  <done>
    - paths.py exists with all 4 public functions + APP_VERSION constant
    - material_library.py no longer imports importlib.resources; uses get_resources_dir()
    - main_window.py uses get_examples_dir() for startup project and file dialog, get_output_dir() for default output, APP_VERSION in title
    - All existing tests pass
    - test_paths.py passes with at least 5 test cases
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite gui.py with splash screen and crash handler</name>
  <files>thermal_sim/app/gui.py</files>
  <action>
    Rewrite `thermal_sim/app/gui.py` with the following structure (per CONTEXT.md locked decisions and RESEARCH.md patterns):

    1. **Crash handler wrapper**: Split into `main()` and `_run_app()`. `main()` wraps `_run_app()` in try/except:
       - On exception: write full traceback to `get_crash_log_path()` using `write_text(encoding="utf-8")`
       - Show `QMessageBox.critical` with error summary (first 800 chars of traceback) and "See crash.log for full details"
       - Use `get_crash_log_path()` from paths.py
       - Do NOT use `print()` anywhere — stdout/stderr are None in windowed mode (RESEARCH.md Pitfall 8)
       - Raise `SystemExit(1)` after handling

    2. **Splash screen** in `_run_app()`:
       - Create `QApplication(sys.argv)` first
       - Apply qt-material theme: `apply_stylesheet(app, theme="dark_amber.xml")`
       - Append `_EXTRA_QSS` to stylesheet (keep existing)
       - Build splash pixmap with QPainter (no external asset file — per research "Don't Hand-Roll" table):
         - `QPixmap(480, 280)` filled with `QColor("#212121")` (dark background matching qt-material)
         - Draw app name "Thermal Simulator" and "v1.0" (use `APP_VERSION` from paths.py) in `QColor("#FFB300")` (dark_amber accent)
         - Use `QFont("Segoe UI", 22, QFont.Bold)` for title, smaller font for version
         - Add "Loading..." message via `splash.showMessage()` at bottom in `QColor("#e0e0e0")`
       - `splash.show()` + `app.processEvents()` to force paint
       - Apply `mpl.rcParams.update(DARK_MPL_STYLE)` (keep existing constant)
       - Import and construct `MainWindow()` AFTER splash is visible (this is the slow part)
       - `window.show()` then `splash.finish(window)` to dismiss
       - `raise SystemExit(app.exec())`

    3. Keep `DARK_MPL_STYLE` dict and `_EXTRA_QSS` string unchanged.

    4. Keep the `if __name__ == "__main__": main()` guard.

    5. Import structure: `sys`, `traceback` at top level. All PySide6/Qt imports inside `_run_app()` (deferred so crash handler can still show a dialog even if PySide6 import partially fails). Import `from thermal_sim.core.paths import get_crash_log_path, APP_VERSION` at top level (paths.py has no heavy deps).
  </action>
  <verify>
    <automated>cd G:/blu-thermal-simulation && python -c "from thermal_sim.app.gui import main; print('import ok')" && py -m pytest -q tests/ -x</automated>
  </verify>
  <done>
    - gui.py has main() crash wrapper calling _run_app()
    - Splash screen created with QPainter (dark bg, amber text, app name + version, "Loading...")
    - Splash dismissed when MainWindow is ready via splash.finish(window)
    - Crash handler writes crash.log and shows QMessageBox on unhandled exception
    - No print() calls anywhere in the file
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `py -m pytest -q tests/ -x` — all tests pass (existing + new test_paths.py)
2. `python -c "from thermal_sim.core.paths import get_resources_dir, get_examples_dir, get_output_dir, get_crash_log_path, APP_VERSION; print(get_resources_dir()); print(get_examples_dir()); print(APP_VERSION)"` — prints valid paths and "1.0"
3. `python -c "from thermal_sim.core.material_library import load_builtin_library; print(len(load_builtin_library()))"` — loads materials without importlib.resources
4. `grep -r "importlib.resources" thermal_sim/` — no matches (fully migrated)
</verification>

<success_criteria>
- paths.py is the single source for all resource path resolution
- No code uses bare relative Path("examples/...") or Path.cwd() for resource lookup
- importlib.resources fully replaced by paths.py
- GUI shows splash on launch, version in title bar, crash.log on fatal error
- All existing tests pass, new path tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution/05-01-SUMMARY.md`
</output>
