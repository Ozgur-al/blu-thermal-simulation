---
phase: 05-distribution
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: []
autonomous: false
requirements: [DIST-01, DIST-02, DIST-03]

must_haves:
  truths:
    - "Double-clicking ThermalSim.exe opens the GUI with splash screen within 10 seconds"
    - "No console window appears"
    - "Splash screen shows dark background, app name, version, Loading... text"
    - "Main window title shows 'Thermal Simulator v1.0'"
    - "Default example project loads correctly from bundled examples/"
    - "File Open dialog defaults to examples/ directory"
    - "Materials load from bundled materials_builtin.json"
    - "Simulation runs and results display correctly"
    - "Output saves to Documents/ThermalSim/outputs/ (not the bundle directory)"
    - "No AV quarantine or UAC prompt on the build machine"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete distribution bundle — all three DIST requirements tested end-to-end.

Purpose: Automated tests verify structure but cannot verify visual behavior (splash appearance, no console flash), managed-machine behavior (AV response, UAC), or real user workflow (double-click launch, run simulation, check output location).

Output: Verified bundle ready for distribution, or specific issues to fix.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-distribution/05-CONTEXT.md
@.planning/phases/05-distribution/05-01-SUMMARY.md
@.planning/phases/05-distribution/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete PyInstaller distribution bundle:
    - ThermalSim.exe with splash screen, crash handler, and version display
    - Bundled examples/ with 3 JSON project files
    - Bundled qt_material theme and materials library
    - Automated build.py pipeline
  </what-built>
  <how-to-verify>
    **DIST-01: Bundle launches with double-click**
    1. Navigate to `dist/ThermalSim/` in Windows Explorer
    2. Double-click `ThermalSim.exe`
    3. Verify: Splash screen appears (dark background, "Thermal Simulator", "v1.0", "Loading...")
    4. Verify: Splash dismisses and main window appears within ~10 seconds
    5. Verify: NO console window appears at any point
    6. Verify: Window title shows "Thermal Simulator v1.0" (or "steady_uniform_stack.json - Thermal Simulator v1.0" if default project loaded)

    **DIST-02: No admin access issues**
    7. Verify: No UAC prompt appeared during launch
    8. Verify: No Windows Defender SmartScreen blocked the exe (if it did, click "More info" > "Run anyway" and note this is expected for unsigned builds)
    9. Run a simulation (F5 or Run button) with the default project
    10. Verify: Results appear in the GUI
    11. Check that output was saved to `C:\Users\<you>\Documents\ThermalSim\outputs\` (not inside the dist/ folder)

    **DIST-03: Resource paths resolve correctly**
    12. Verify: File > Open dialog starts in the `examples/` directory (inside the bundle)
    13. Open `localized_hotspots_stack.json` from the examples folder — verify it loads correctly
    14. Verify: Materials table shows built-in materials (Aluminum, Copper, FR4, etc.)
    15. Close the app and relaunch — verify it starts again without issues

    **Bonus: Zip distribution**
    16. Extract `ThermalSim-v1.0.zip` to a DIFFERENT directory (e.g., Desktop)
    17. Double-click the extracted `ThermalSim/ThermalSim.exe` — verify it works from the new location
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All DIST requirements verified by human tester interacting with the actual built bundle.
</verification>

<success_criteria>
- Phase 5 is complete when human confirms all 17 verification steps pass
- Any issues found become gap closure tasks
</success_criteria>

<output>
After completion, create `.planning/phases/05-distribution/05-03-SUMMARY.md`
</output>
