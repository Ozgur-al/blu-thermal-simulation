---
phase: 04-polish
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false
requirements:
  - PLSH-01
  - PLSH-02
  - PLSH-03

must_haves:
  truths:
    - "All three PLSH requirements confirmed by human tester in live GUI"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all Phase 4 Polish requirements. The tester confirms the qt-material dark amber theme, dockable panel layout, and inline validation feedback all work correctly in the live GUI.

Purpose: Visual and interaction polish cannot be fully verified by automated tests. A human confirms the theme looks correct, docks undock/resize/persist, and validation feedback appears correctly.

Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish/04-CONTEXT.md
@.planning/phases/04-polish/04-01-SUMMARY.md
@.planning/phases/04-polish/04-02-SUMMARY.md
@.planning/phases/04-polish/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 Polish: qt-material dark amber theme (PLSH-01), QDockWidget layout (PLSH-02), and inline validation feedback (PLSH-03).
  </what-built>
  <how-to-verify>
Launch the GUI and verify all three requirements:

```bash
cd G:\blu-thermal-simulation
python -m thermal_sim.app.gui
```

**PLSH-01: Theme verification**
1. Confirm the overall look is dark gray with amber/gold accents — no white/light OS-default widgets visible
2. Check menus (File, Edit, Run, View) are dark-themed
3. Check table cells use monospace font (Consolas or similar)
4. Check status bar values use monospace font
5. Open an example project and run a simulation: confirm the temperature map uses inferno colormap on a dark background (#212121/#303030)
6. Verify probe history plot lines use amber palette colors (gold, orange, green, blue, purple)
7. Export a PDF report and open it: confirm the PDF has light/white backgrounds (not dark) suitable for printing

**PLSH-02: Dockable layout verification**
1. Confirm three dock panels are visible: "Editor" (left), "Result Plots" (top-right), "Results Summary" (bottom-right)
2. Undock the "Result Plots" panel by double-clicking its title bar or dragging — confirm it floats as an independent window
3. Re-dock it by dragging back to the right area
4. Resize the Editor panel by dragging the dock divider
5. Open View menu: confirm checkable entries for Editor, Result Plots, Results Summary, plus "Reset Layout"
6. Uncheck "Editor" in View menu — confirm the panel hides. Re-check to show it.
7. Click "Reset Layout" — confirm panels return to default arrangement
8. Rearrange docks to a custom layout, close the app, relaunch — confirm the custom layout is restored
9. Click "Reset Layout" to restore default — confirm it works after a restart

**PLSH-03: Inline validation verification**
1. In the Materials tab, change "k in-plane" to "0" — confirm the cell turns dark red with tooltip "k in-plane must be > 0"
2. Change it to "-5" — confirm the cell stays red with updated tooltip
3. Change it back to "200" — confirm the cell clears to normal appearance
4. Enter "abc" in a numeric field — confirm "not a valid number" tooltip
5. Check the status bar: when errors exist, it should show "N validation errors"
6. Confirm the Run button (F5 or toolbar) is disabled when validation errors exist
7. Fix all errors — confirm the Run button re-enables and status bar shows "Ready"
8. Load a new project (File > Open) — confirm all validation errors clear
9. In Layers tab, set Thickness to "-0.001" — confirm red cell with "must be > 0"
10. Delete the invalid row — confirm the error clears and Run button re-enables
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human confirms all three PLSH requirements pass visual and interaction testing.
</verification>

<success_criteria>
- Human approves PLSH-01: dark amber theme consistent across all widgets, matplotlib dark plots, PDF light export
- Human approves PLSH-02: docks undock/resize/persist, View menu works, Reset Layout works
- Human approves PLSH-03: inline validation shows red cells with tooltips, run button disabled on errors, errors clear correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish/04-04-SUMMARY.md`
</output>
