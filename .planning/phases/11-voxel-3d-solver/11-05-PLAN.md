---
phase: 11-voxel-3d-solver
plan: 05
type: execute
wave: 4
depends_on: [11-02, 11-03, 11-04]
files_modified:
  - tests/test_voxel_solver.py
  - tests/test_conformal_mesh.py
  - tests/test_voxel_assignment.py
autonomous: false
requirements: [VOX-01, VOX-02, VOX-03, VOX-04, VOX-05, VOX-06, VOX-07, VOX-08, VOX-09, VOX-10, VOX-11, VOX-12, VOX-13, VOX-14]

must_haves:
  truths:
    - "All pytest tests pass with no failures or errors"
    - "DLED example runs steady-state solve from CLI and produces temperature output"
    - "ELED example runs steady-state solve from CLI and produces temperature output"
    - "GUI launches without import errors"
    - "3D view renders ELED assembly showing metal frame, air gaps, FR4 PCBs, and LGP as distinct blocks"
  artifacts:
    - path: "tests/test_voxel_solver.py"
      provides: "Analytical validation tests passing for new solver"
      contains: "test_1d_two_layer_resistance"
    - path: "tests/test_conformal_mesh.py"
      provides: "Mesh generation tests passing"
      contains: "test_overlapping_blocks"
  key_links:
    - from: "thermal_sim/app/cli.py"
      to: "examples/eled_voxel.json"
      via: "CLI loads and solves example project"
      pattern: "load_voxel_project"
---

<objective>
Final integration verification: run all tests, verify CLI with both examples, and verify GUI launches.

Purpose: Confirm the entire voxel-based 3D solver stack works end-to-end before declaring Phase 11 complete.
Output: All tests pass, both examples solve correctly, GUI functional.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-voxel-3d-solver/11-CONTEXT.md
@.planning/phases/11-voxel-3d-solver/11-01-SUMMARY.md
@.planning/phases/11-voxel-3d-solver/11-02-SUMMARY.md
@.planning/phases/11-voxel-3d-solver/11-03-SUMMARY.md
@.planning/phases/11-voxel-3d-solver/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix any remaining test failures and import errors</name>
  <files>
    tests/test_voxel_solver.py,
    tests/test_conformal_mesh.py,
    tests/test_voxel_assignment.py
  </files>
  <action>
    Run the full test suite and fix any failures:

    ```bash
    py -m pytest -q tests/
    ```

    Common issues to check and fix:
    1. Import errors from deleted modules — find and fix all remaining references to old Layer/DisplayProject/network_builder code
    2. Conftest fixtures that reference old models — update or remove
    3. Test files that weren't deleted but should have been
    4. Any surviving tests that reference old HeatSource, LEDArray, etc.

    Also verify:
    ```bash
    python -m thermal_sim.app.cli --mode steady --project examples/dled_voxel.json --output-dir outputs
    python -m thermal_sim.app.cli --mode steady --project examples/eled_voxel.json --output-dir outputs
    ```

    Fix any issues found. If the ELED solve reveals air-gap convergence problems, increase the fill_factor in spilu or adjust the minimum conductance floor as described in the RESEARCH.md pitfall section.
  </action>
  <verify>
    <automated>py -m pytest -q tests/ && python -m thermal_sim.app.cli --mode steady --project examples/dled_voxel.json --output-dir outputs</automated>
  </verify>
  <done>All tests pass. Both DLED and ELED examples solve from CLI without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GUI and 3D view</name>
  <files>thermal_sim/ui/main_window.py</files>
  <action>
    Human verification of the complete voxel-based 3D thermal simulator:
    - Block editor for defining assembly geometry
    - Surface source placement for LEDs
    - 3D PyVista view with slice planes, transparency, and threshold
    - Solver producing correct analytical results
    - DLED and ELED example projects

    Steps to verify:
    1. Launch GUI: `python -m thermal_sim.app.gui`
    2. Open the ELED example: File > Open > examples/eled_voxel.json
    3. Verify block editor shows all blocks (metal frame, LGP, FR4 PCBs) with correct dimensions in mm
    4. Verify 3D preview shows the assembly with distinct color-coded blocks
    5. Click Run (F5) to solve steady-state
    6. Verify 3D temperature overlay appears with color scale
    7. Use the Z-slice slider to cut through the assembly and inspect internal temperatures
    8. Toggle block visibility to hide the metal frame and see internal structure
    9. Apply temperature threshold to highlight hot spots
    10. Check that ELED model shows two heat paths: LED->FR4->metal (primary) and LED->air->LGP (secondary)
  </action>
  <verify>
    <automated>python -c "from thermal_sim.ui.main_window import MainWindow; print('GUI importable')"</automated>
  </verify>
  <done>User has visually verified the GUI renders correctly with block editor, 3D view, and temperature results for the ELED example project.</done>
</task>

</tasks>

<verification>
- `py -m pytest -q tests/` passes with zero failures
- CLI solves both DLED and ELED examples
- GUI launches and renders 3D view
- ELED temperature pattern shows expected two-heat-path behavior
</verification>

<success_criteria>
The complete voxel-based 3D thermal simulator is functional: models load, solver produces correct physics, GUI displays results in 3D, and the user has verified the ELED visualization matches expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/11-voxel-3d-solver/11-05-SUMMARY.md`
</output>
