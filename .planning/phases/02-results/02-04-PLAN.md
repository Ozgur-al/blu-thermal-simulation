---
phase: 02-results
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified: []
autonomous: false
requirements:
  - RSLT-01
  - RSLT-02
  - RSLT-03
  - RSLT-04
must_haves:
  truths:
    - "All four RSLT requirements verified by human"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all Phase 2 Results requirements.

Purpose: Visual and functional verification that all four requirements work correctly in the live GUI before marking Phase 2 complete.

Output: Human confirmation that all success criteria pass.
</objective>

<execution_context>
@C:/Users/hasan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hasan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-results/02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify all Phase 2 Results requirements</name>
  <files></files>
  <action>
Launch the GUI and verify all 4 RSLT requirements interactively. Complete Phase 2 Results: structured results summary table, hotspot map annotations with click-to-navigate, PDF report export, and named result snapshot comparison.
  </action>
  <verify>
    <automated>cd G:/blu-thermal-simulation && py -m pytest -q tests/ -x</automated>
  </verify>
  <done>All 4 RSLT requirements confirmed working by human tester</done>
  <how-to-verify>
Launch the GUI: `python -m thermal_sim.app.gui`

**RSLT-01 -- Results Summary Table:**
1. Load an example project (e.g., `examples/localized_hotspots_stack.json`)
2. Click "Run Simulation" (steady-state mode)
3. Verify the Results tab auto-activates after simulation
4. Verify the per-layer stats table shows Layer, T_max, T_avg, T_min, DeltaT columns with values for each layer
5. Verify the hotspot ranking table shows Rank, Layer, X [mm], Y [mm], Temperature columns with up to 10 entries
6. Verify the probe readings table shows Probe, Layer, X [mm], Y [mm], Temperature columns

**RSLT-02 -- Hotspot Map Annotations:**
7. Switch to the Temperature Map tab
8. Verify thin crosshair lines appear at hotspot locations with rank numbers and temperature labels
9. Verify probe locations appear as diamond markers with names
10. Click a hotspot row in the Results tab -- verify the map switches to the correct layer and highlights the clicked hotspot
11. Change the layer dropdown -- verify the crosshairs update to show that layer's top 3 hotspots

**RSLT-03 -- PDF Export:**
12. Click "Export PDF"
13. Choose a save location
14. Open the generated PDF and verify it contains:
    - First page: project name, simulation date, mode, per-layer stats table
    - Middle pages: one temperature map per layer with hotspot annotations
    - Hotspot ranking table page
    - (If transient): probe history plot page

**RSLT-04 -- Snapshots and Comparison:**
15. Click "Save Snapshot" and name it "baseline"
16. Modify a parameter (e.g., change a layer thickness) and re-run
17. Click "Save Snapshot" and name it "modified"
18. Switch to the Comparison tab
19. Select both snapshots and click "Compare"
20. Verify the metric comparison table shows values for both snapshots with a delta column
21. Verify the probe overlay plot shows curves from both snapshots in different colors
22. Verify the side-by-side temperature maps show both snapshots' maps for the same layer
23. (Bonus) Save a 5th snapshot and verify the oldest is evicted

**Also verify:**
24. Run a transient simulation and repeat steps 3-6 to verify Results tab works for transient mode
25. Verify probe history overlay shows time series (not just points) for transient comparison
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all four RSLT requirements work correctly in the live GUI.
</verification>

<success_criteria>
- RSLT-01: Results tab shows structured per-layer metrics and hotspot ranking after simulation
- RSLT-02: Temperature maps display hotspot crosshairs and probe markers; click-to-navigate works
- RSLT-03: PDF export produces a complete multi-page engineering report
- RSLT-04: Named snapshots can be saved, selected, and compared with metrics, probes, and maps
</success_criteria>

<output>
After completion, create `.planning/phases/02-results/02-04-SUMMARY.md`
</output>
